<?php
require_once __DIR__ . "/conexao.php";

/*
 * TAREFA:
 * - Consultar todos os registros da tabela "carros" no banco SQLite.
 * - Caso não existam registros, exibir um parágrafo:
 *      "Nenhum carro encontrado."
 * - Caso existam, exibir uma tabela HTML com as colunas:
 *      Modelo | Ano | Preço
 * 
 * DICAS:
 * - Use um SELECT simples, como: SELECT * FROM carros
 * - Utilize fetchAll() para obter todos os registros.
 * - Formate o preço com number_format().
 * - Lembre-se de escapar os valores ao exibir no HTML.
 */

// Seu código aqui ↓

?>
